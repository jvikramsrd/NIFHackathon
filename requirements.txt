# ══════════════════════════════════════════════════════════════════════════════
# Geo-Intel A4000 Pipeline — Requirements
# ══════════════════════════════════════════════════════════════════════════════
#
# INSTALL ORDER (important on Windows):
#
#   Step 1 — PyTorch with CUDA 12.1 (for RTX A4000):
#     pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
#   Step 2 — Geospatial stack (conda recommended for ECW support):
#     conda install -c conda-forge libgdal-ecw rasterio fiona geopandas
#     OR (pip only, no ECW):
#     pip install rasterio fiona geopandas
#
#   Step 3 — Everything else:
#     pip install -r requirements.txt
#
#   Step 4 — Optional CRF (requires VS Build Tools 2022):
#     pip install pydensecrf2
# ══════════════════════════════════════════════════════════════════════════════

# ── Deep Learning ─────────────────────────────────────════════════════════════
segmentation-models-pytorch>=0.3.3
timm>=0.9.12
ultralytics>=8.1.0

# ── Geospatial (install via conda for ECW — see above) ────────────────────────
shapely>=2.0.3
pyproj>=3.6.1
# rasterio, fiona, geopandas installed via conda above

# ── Image Processing / Augmentation ──────────────────────────────────────────
albumentations>=1.4.0
opencv-python>=4.9.0
Pillow>=10.2.0

# ── Instance Separation (Watershed) ──────────────────────────────────────────
scikit-image>=0.22.0

# ── Small Object Detection ────────────────────────────────────────────────────
sahi>=0.11.15           # Slicing Aided Hyper Inference for transformers/wells

# ── Data / ML Utilities ───────────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.12.0
scikit-learn>=1.4.0
tqdm>=4.66.1
pyyaml>=6.0.1
matplotlib>=3.8.0
lxml

# ── Hardware / System ─────────────────────────────────────────────────────────
psutil>=5.9.0        # P-core affinity pinning for i9-13900K DataLoader workers

# ── Logging ───────────────────────────────────────────────────────────────────
tensorboard>=2.16.0

# ── GUI ───────────────────────────────────────────────────────────────────────
PyQt6>=6.6.0

# ── ONNX Export (graph only — runtime is optional, see notes below) ──────────
onnx>=1.16.0

# ── Optional dependencies ─────────────────────────────────────────────────────
# Test runner — install via:    pip install -e .[dev]
# CRF refinement —              pip install pydensecrf2
# ONNX GPU runtime —            pip install onnxruntime-gpu  (or onnxruntime for CPU/AMD)
# TensorRT —                    pip install nvidia-tensorrt --index-url https://pypi.ngc.nvidia.com
#
# These were previously pinned in this file. Forcing onnxruntime-gpu in core
# broke fresh installs on CPU-only and AMD machines; install.sh and
# setup_venv.bat both had workarounds for it. Pulled out to keep the base
# install always succeed.
